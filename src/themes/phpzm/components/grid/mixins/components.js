import { QDialogSelect, QSearch, QPagination } from 'quasar-framework'
import QButton from 'src/themes/phpzm/components/button/AppButton.vue'

export default {
  components: {
    QButton, QDialogSelect, QSearch, QPagination
  }
}
